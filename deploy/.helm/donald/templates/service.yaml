apiVersion: v1
kind: Service
metadata:
  name: {{ include "donald.fullname" . }}
  labels:
    {{- include "donald.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "donald.selectorLabels" . | nindent 4 }}
