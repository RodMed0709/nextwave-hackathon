apiVersion: v1
kind: Service
metadata:
  name: {{ include "donald-web.fullname" . }}
  labels:
    {{- include "donald-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "donald-web.selectorLabels" . | nindent 4 }}
