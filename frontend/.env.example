# Copy to .env.local (gitignored). Without it the app plays bundled recordings only.
# The backend is deployed and public — never point this at localhost/127.0.0.1.
NEXT_PUBLIC_DONALD_API=https://api.usedonald.com
